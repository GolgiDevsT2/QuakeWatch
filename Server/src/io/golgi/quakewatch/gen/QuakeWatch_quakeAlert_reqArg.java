/* IS_AUTOGENERATED_SO_ALLOW_AUTODELETE=YES */
/* The previous line is to allow auto deletion */

package io.golgi.quakewatch.gen;

import java.util.Hashtable;
import java.util.ArrayList;
import java.util.Iterator;
import com.openmindnetworks.golgi.JavaType;
import com.openmindnetworks.golgi.GolgiPayload;
import com.openmindnetworks.golgi.B64;
import com.openmindnetworks.golgi.api.GolgiException;
import com.openmindnetworks.golgi.api.GolgiAPI;

public class QuakeWatch_quakeAlert_reqArg
{

    private boolean corrupt = false;

    public boolean isCorrupt() {
        return corrupt;
    }

    public void setCorrupt() {
        corrupt = true;
    }

    private boolean detailsIsSet = false;
    private QuakeDetails details;

    public QuakeDetails getDetails(){
        return details;
    }
    public boolean detailsIsSet(){
        return detailsIsSet;
    }
    public void setDetails(QuakeDetails details){
        this.details = details;
        this.detailsIsSet = true;
    }

    public StringBuffer serialise(){
        return serialise(null);
    }

    public StringBuffer serialise(StringBuffer sb){
        return serialise("", sb);
    }

    public StringBuffer serialise(String prefix, StringBuffer sb){
        if(sb == null){
            sb = new StringBuffer();
        }

        if(this.detailsIsSet){
            details.serialise(prefix + "" + 1 + "." , sb);
        }

        return sb;
    }

    private void deserialise(GolgiPayload payload){
        if(!isCorrupt() && payload.containsNestedKey("1")){
            QuakeDetails inst = new QuakeDetails(payload.getNested("1"));
            setDetails(inst);
               if(inst == null || inst.isCorrupt()){
                   setCorrupt();
               }
        }
        else {
            setCorrupt();
        }
    }

    public QuakeWatch_quakeAlert_reqArg(){
        this(true);
    }

    public QuakeWatch_quakeAlert_reqArg(boolean isSetDefault){
        super();
        detailsIsSet = isSetDefault;
        details = new QuakeDetails(isSetDefault);
    }

    public QuakeWatch_quakeAlert_reqArg(GolgiPayload payload){
        this(false);
        deserialise(payload);
    }

    public QuakeWatch_quakeAlert_reqArg(String payload){
        this(JavaType.createPayload(payload));
    }

}
